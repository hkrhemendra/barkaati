import React from 'react';
import Footer from '../Footer';
import Header from '../Header';
import SectionStart from '../SectionStart';

const Blogs = () => {
    return (
        <div>
            <Header/>
            <SectionStart title = "Blogs" address="blogs"/>
            <Footer/>
        </div>
    );
}

export default Blogs;
